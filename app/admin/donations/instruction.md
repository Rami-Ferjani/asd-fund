read @context/project-overview.md  and @context/architecture-context.md  and @context/DESIGN.md. Your job is to create an implementation plan for the donations page of the admin dashboard for an  ai model to implement.put your implementation plan inside @app/admin/donations/implementation.md  .instruct the model to use shadcn component in the design and tailwind css for styling. if a needed shadcn component isn't installed instruct the ai model to install it.instruct to only use existing convex function and not add new ones.if something new is needed in the backend tell me first.the ai job is to implement the donation part of the dashboard .the design is currently an html file @app/admin/screenshots/donations.html your job is to create the implementation so that the ai implements the design from @app/admin/screenshots/donations.html  to become a next js components that interacts with the convex backend. instruct the ai which colors to use and which shadcn components to use and which convex function to use .the convex function are at @convex/donors.ts  and @convex/donations.ts.instruct the ai to only use existing convex function without adding new ones. the side bar is already implemented in @app/admin/layout.tsx which is set the same side bar for all three router of admin ./donor and /donations  and /info.instruct ai to implement the remaining part of the ui. instruct the ai not to add any auth whatsoever because that will be done in a later task
the ui of donations management is a crud for managing donations. each donations has a donor and amount. the admin chooses an existing donor .if the donor doesn't exist yet he may press add new donor to create w new donor. .when the admin press the add new donor a pop up should apear which contain text field for donor name and phone number and a text field for url of profile pic. tell the ai not to add file upload for the profile picture since that will be a future task. 
if you need to add colors as tokens in tailwind css  or shadcn you may do so if it helps.the design also has a table the shows recent donation but missing two buttons edit and delete for each new donation so add them.
5. **Scope:** implement **only the donations management page** (`app/admin/donations/page.tsx`) . Do **not** touch `app/admin/info/page.tsx` or `app/admin/donors/page.tsx` (they are stubs for a separate task).

The design in @app/admin/screenshots/donations.html isn't optimized for mobile view so instruct the ai to make this Make this section truly mobile-first, not just responsive. At each Tailwind breakpoint (sm, md, lg), adapt the layout rather than simply shrinking it. Stack headers and toolbars vertically on small screens, make inputs full-width, reduce padding and font sizes appropriately, and replace desktop tables with mobile-friendly card layouts below the md breakpoint. Ensure every section feels intentionally designed for phones with comfortable touch targets and no horizontal scrolling except where absolutely unavoidable.you may modify the side bar in @app/admin/layout.tsx to make it responsive and mobile first. but keep in mind that two other component use it so dont ruin the desktop view just add clases to the side bar to make it better for the mobile first.I m still gonna go back and modify the other two component at `app/admin/info/page.tsx` and `app/admin/donors/page.tsx`  to make them mobile first too so don't modify them now since its a future task