read @context/project-overview.md  and @context/architecture-context.md  and @context/DESIGN.md . Your job is to create an implementation plan for the donor page of the admin dashboard for an  ai model to implement.put your implementation plan inside @app/admin/donors/implementation.md  .instruct the model to use shadcn component in the design and tailwind css for styling. if a needed shadcn component isn't installed instruct the ai model to install it.instruct to only use existing convex function and not add new ones. his job is to implement the dashboard  .the design is currently an html file @app/admin/screenshots/donors.html your job is to create the implementation so that the ai implements the design from @app/admin/screenshots/donors.html  to become a next js components that interacts with the convex backend. instruct the ai which colors to use and which shadcn components to use and which convex function to use .the convex function are at @convex/donors.ts  and @convex/donations.ts.instruct the ai to only use existing convex function without adding new ones. the side bar is already implemented in @app/admin/layout.tsx which is set the same side bar for all three router of admin ./donor and /donations  and /info.instruct ai to implement the remaining part of the ui. instruct the ai not to add any auth whatsoever because that will be done in a later task
the ui of donor management is a crud for mamaging donors. the donor has a name. phone number and a number of donoations .when the admin press the add new donor a pop up should apear which contain text field for donor name and phone number and a text field for url of profile pic. tell the ai not to add file upload for the profile picture since that will be a future task. donations arent added here only the donor info. donations will be added in the add donations tab in later task.tell the ai not to calculate total donation in the frontend as  it is calculated in the backend the ai needs to use the exisitng convex function
if you need to add colors as tokens in tailwind css  or shadcn you may do so if it helps
5. **Scope:** implement **only the donors management page** (`app/admin/donors/page.tsx`) . Do **not** touch `app/admin/info/page.tsx` or `app/admin/donations/page.tsx` (they are stubs for a separate task).